assertions:
  - name: "show alpha-task has labels a, c"
    command: "tisket issue show alpha-task | grep -q 'a, c'"
  - name: "label values are exactly a, c"
    command: "test \"$(tisket issue show alpha-task | grep 'Labels:' | sed 's/.*Labels: *//')\" = 'a, c'"
