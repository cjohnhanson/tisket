# tisket

Plaintext issue tracker for humans and coding agents.

Issues are markdown files with YAML frontmatter, stored in `.tisket/` directories. Git-aware divergence detection shows when on-disk state differs from branch state. Full-text search across titles, bodies, and scratch notes.
